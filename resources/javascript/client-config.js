'use strict';
// Initialize scripts with config from backend where resources are maintained...
window.atlasDistTestConfig = {
	configParams: function(params){
		window.atlasDistTestConfig.params = params;
	},
	params: {}
}